# Escolha UMA das URLs abaixo deixando apenas uma descomentada

# Atlas (ativo por padrão)
MONGOURL="mongodb+srv://<usuario>:<senha>@<seu-cluster>/?appName=<appName>"

# Local (comente a linha do Atlas acima e descomente esta para usar o Mongo local)
# MONGOURL="mongodb://localhost:27017"
